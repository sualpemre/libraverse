server:
  port: 8888

spring:
  application:
    name: CONFIG-SERVER
  profiles:
    active: composite

  cloud:
    config:
      server:
        composite:
          - type: native
            search-locations: classpath:/settings